const findMax = (numbers) => {
    let maxLength = 0;
    
    for (let i = 0; i < str.length; i++) {
        if (str[i].length > max) {
            max = str[i];
        }
    }
    
    return longestWord;
}
  
 console.log(findMax([1, 0, -5, 40, 10, -100, 48])); 
